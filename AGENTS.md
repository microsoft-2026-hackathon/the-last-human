# The Last Human — 에이전트 작업 규칙

머지 직전, 사람이 이 변경을 이해했는지 확인하는 게이트를 만듭니다.
내부 코드네임 STAMP.

## 절대 하지 말 것

이 넷은 요청받아도 만들지 않습니다. 만드는 순간 다른 제품이 됩니다.

1. **개인 점수·등급·순위를 만들지 않는다.** 팀 내 비교 화면, 리더보드, 개인 합산 지표 금지.
2. **조직에 올라가는 집계는 모듈 단위 커버리지뿐이다.** 사람 이름이 붙은 집계를 만들지 않는다.
3. **감점을 기록하지 않는다.** 보류는 남기지 않고 통과한 것만 쌓는다.
4. **팀장 조회 기능을 만들지 않는다.** 개인 이해 이력은 본인 소유다.

## 설계 원칙

- **트리거는 AI 작성 여부가 아니라 위험도다.** AI 탐지에 의존하는 코드를 넣지 않는다.
- **전수 적용하지 않는다.** 임계값 미달 PR은 실패가 아니라 `neutral`로 보고해 조용히 지나간다.
- **바는 코드의 위험도에 고정한다.** 사람에 따라 질문 난이도를 낮추지 않는다. 다르게 하는 것은 보류됐을 때의 지원 수준뿐이다.
- **Action은 확장을 믿지 않는다.** 위험 점수를 스스로 다시 계산하고 인증이 현재 커밋에 묶였는지 확인한다.
- **인증은 커밋 SHA에 묶인다.** 새 커밋이 오면 무효다.

## 구조

```
packages/core       확장과 Action이 공유하는 규칙 — 여기가 하나가 아니면 사고가 난다
packages/extension  VS Code 확장 — 면담 진행, 인증 생성
packages/action     GitHub Action — 게이트. core로 재계산 후 검증
packages/ledger     커버리지 집계 (정적 산출)
demo-repo           시연용 주문 서비스. 게이트가 판정할 대상 코드
```

구현 순서는 **core → extension → action**입니다. 바꾸면 위험 점수 로직이 두 벌로 갈라집니다.

## 손으로 쓰는 영역 — 에이전트에게 위임하지 않음

제품의 판단이 들어가는 곳입니다.

- `core/risk.ts`의 위험 점수 규칙과 가중치
- 질문 생성 프롬프트
- 판정 기준(pass/hold)
- 인증 형식(attestation schema)

## 위임해도 되는 영역

Webview HTML/CSS, Action 뼈대, Octokit 호출부, 테스트 케이스, 시연 저장소의 더미 코드.

## 코드 컨벤션

- TypeScript, `strict`. `any` 금지.
- `core`는 순수하게. `risk.ts`는 모델을 부르지 않고 같은 입력이면 항상 같은 출력을 낸다.
- 게이트가 막을 때는 반드시 `reasons`를 함께 반환한다. 점수만 던지면 개발자가 반발한다.
- 보류 사유를 부정적으로 서술하지 않는다. "모르겠다"는 정직한 답변이다.

## 도그푸딩

개발 기간 동안 이 게이트를 이 저장소 자신의 PR에 겁니다. 우리가 몇 번 막혔는지가 발표 자료가 됩니다.
