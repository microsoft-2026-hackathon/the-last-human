# 발표 자료 — The Last Human · 3분 30초

해커톤 발표용 덱입니다. **88초 데모 영상을 가운데 두고 앞 68초 · 뒤 54초**를 슬라이드가 맡습니다.

구성 근거와 슬라이드별 발화는 [`../storyboard-v5.md`](../storyboard-v5.md)에 있습니다. 이 README는 **띄우고 넘기는 방법**만 다룹니다.

## 파일

| 파일 | 무엇 |
| --- | --- |
| [`index.html`](index.html) | **본 덱.** 본편 14장(210초) + Q&A 부록 3장. 브라우저로 열면 바로 발표 가능 |
| [`architecture-simple.html`](architecture-simple.html) | 아키텍처 요약 — 슬라이드 08과 같은 SVG를 단독 페이지로 |
| [`architecture-details.html`](architecture-details.html) | 아키텍처 상세 — 9단계 데이터 흐름 · 신뢰 경계 넷. **Q&A 전용** |
| [`verify_claims.py`](verify_claims.py) | 슬라이드의 위험 점수를 실제 채점기로 재현 |
| [`cases/`](cases/) | 재현에 쓰는 diff 3종 |
| `Demo_thelasthuman.mp4` | 데모 영상 **88.22초**. 데모 슬라이드에서 자동 재생 (git에는 올리지 않음) |

## 띄우기

```bash
# 저장소 루트에서
start docs/demo/presentation/index.html      # Windows
open  docs/demo/presentation/index.html      # macOS
```

폰트는 Google Fonts에서 받아옵니다. **발표장 네트워크가 불안하면 미리 한 번 열어 캐시**해 두세요. 폰트를 못 받아도 내용은 시스템 폰트로 정상 표시됩니다.

## 조작

| 키 | 동작 |
| --- | --- |
| `→` `Space` / `←` | 다음 / 이전 |
| `F` | 전체화면 |
| `A` | **부록으로 점프** — A1 아키텍처 상세 · A2 고객과 가치 · A3 배포·확장·Global |
| `M` | 우측 상단 타이머 숨기기 — 화면 공유 전에 끄세요 |
| `H` | 좌측 하단 안내 숨기기 |
| `Home` / `End` | 처음 / 본편 마지막 |

우측 상단 타이머는 **각 슬라이드의 목표 구간**(예: `1:08 → 2:36 · 88s`)을 보여 줍니다. 리허설에서 이 구간을 넘기면 그 슬라이드의 말이 긴 것입니다.

## 데모 영상

`Demo_thelasthuman.mp4`가 이 폴더에 있으면 데모 슬라이드에 도달할 때 자동 재생됩니다. 파일이 없으면 10개 큐가 적힌 카드가 대신 뜹니다.

**이 파일은 git에 올리지 않았습니다**(12.7MB). 팀원과 공유하려면 별도로 전달하거나 저장소에 추가하세요.

**데모 중에는 말하지 않습니다.** 심사 배점에서 시연이 30점으로 가장 크고, 해설을 얹으면 "동작 결과물"이 아니라 "설명"으로 읽힙니다.

실측 길이는 **88.22초**로 덱의 `data-dur="88"`과 맞습니다. 영상을 교체하면 이 값을 실제 길이로 바꾸세요 — 타이머만 따라 바뀌고 다른 슬라이드는 그대로입니다.

## 발표 전 점검

```bash
# 1. 슬라이드의 위험 점수가 현재 규칙과 맞는지
PYTHONIOENCODING=utf-8 python docs/demo/presentation/verify_claims.py
```

세 줄 모두 `[OK ]`가 나와야 합니다. `불일치`가 뜨면 `.lasthuman.yml`이 바뀐 것이므로 **슬라이드 10의 `30 / 40`을 고치고** 발표하세요. 화면의 숫자와 코드가 어긋난 채 시연하면 그 자리에서 무너집니다.

그 외:

- [ ] 슬라이드 07의 게이트 이력(26 · 15 · 14 · 3)은 `hunhoon21/the-last-human`의 값이다 — 이관 사실을 물으면 `docs/migration/import.json`을 보여 준다
- [ ] 외부 출처 4종의 링크가 살아 있는가 (Godot · 연구 · New Relic · GitHub 8·17 장애)
- [ ] 대시보드 화면을 띄운다면 `Demo data` 칩이 보이는가
- [ ] 타이머(`M`)와 안내(`H`)를 껐는가

## 고칠 때

- **슬라이드 문구·시간** → `index.html`의 해당 `<section>`. `data-dur`가 그 슬라이드의 초
- **아키텍처 그림** → `architecture-simple.html`이 원본입니다. `index.html` 슬라이드 08에 같은 SVG가 들어가 있으니 **한쪽을 고치면 다른 쪽도 맞추세요**
- **서사·발화·근거** → `../storyboard-v5.md`를 먼저 고치고 덱을 따라오게 합니다

## 주의

이 덱은 [`AGENTS.md`](../../../AGENTS.md)의 금지 넷 — 개인 점수·순위, 이름이 붙은 집계, 감점 기록, 팀장 조회 — 을 넘지 않습니다. **심사 점수를 올리려고 이 넷을 넘지 마세요.** 넘는 순간 다른 제품이 됩니다.
