## 배경

외부 인증 서버에서 429가 관측된다는 이슈(#12)를 보고, 클라이언트 쪽에서 먼저 요청량을 흘려보내는 편이 낫겠다고 판단했습니다.

## 변경 내용

- `src/http/rate-limit.js` — 토큰 버킷 제한기. 용량 20, 초당 5개 리필.
- `src/http/client.js` — `postJson()`이 나가기 전에 버킷을 통과하도록 했습니다.

## 테스트

`npm test` 통과 (5/5).

---

이 저장소에 처음 기여합니다. 컨벤션이 다르면 알려주세요.
