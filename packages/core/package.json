{
  "name": "@lasthuman/core",
  "version": "0.1.0",
  "private": true,
  "description": "확장과 Action이 공유하는 규칙 — diff 파싱, 위험 점수, 인증 형식",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "lasthuman-diff": "dist/cli/diff-cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "minimatch": "^9.0.4",
    "parse-diff": "^0.11.1",
    "yaml": "^2.4.2"
  }
}
